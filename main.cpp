#include "tests/test.hpp"
// #include "RedBlackTree/RedBlackTree.hpp"
// #include "utils/pair.hpp"
// #include "vector/vector.hpp"
// #include <cstdlib>
// #include <iostream>
// #include <time.h>
// # include <vector>
// # include <map>

// using namespace	ft;

int	main(void) {
	// vector_test();
	// stack_test();
	map_test();
	// RedBlackTree<int>	rbt;
	// int		num_to_insert = 50;
	// int		num_range = 1000;

	// srand(time(0));
	// for (int i = 0; i < num_to_insert; i++)
	// {
	// 	int random_num = rand() % num_range;
	// 	cout << "============" << endl;
	// 	cout << "num to insert : " << random_num << endl;
	// 	rbt.insert_node(random_num);
	// 	rbt.print_tree(rbt.get_root());
	// 	cout << "============" << endl;
	// }

	// rbt.insert_node(5);
	// rbt.insert_node(6);
	// rbt.insert_node(26);
	// rbt.insert_node(18);
	// rbt.insert_node(16);
	// rbt.insert_node(19);
	// rbt.insert_node(7);
	// rbt.insert_node(8);
	// rbt.insert_node(15);
	// rbt.insert_node(17);
	// rbt.insert_node(25);
	// rbt.insert_node(1);
	// rbt.insert_node(2);
	// rbt.insert_node(3);
	// rbt.insert_node(4);

	// rbt.print_tree(rbt.get_root());
	// rbt.clear_tree(rbt.get_root());
	// rbt.print_tree(rbt.get_root());
	// rbt.delete_node(15);
	// rbt.print_tree(rbt.get_root());

	// RedBlackTree<pair<int, int> >	rbt;

	// rbt.insert_node(ft::make_pair(2, 2));
	// rbt.insert_node(ft::make_pair(2, 3));
	// rbt.insert_node(ft::make_pair(2, 4));

	// rbt.print_tree(rbt.get_root());

	// std::map<int, int>	m;

	// m.insert(std::make_pair(42, 42));
	// m.insert(std::make_pair(42, 41));
	// m.insert(std::make_pair(42, 40));

	// for (std::map<int, int>::iterator it = m.begin(); it != m.end(); it++) {
	// 	std::cout << it->first << std::endl;
	// }

	// std::vector<int>	v(5, 10);
	// std::map<std::string, int>	m(v.begin(), v.end());

	// for (std::map<std::string, int>::iterator it = m.begin(); it != m.end(); it++) {
	// 	std::cout << it->first << std::endl;
	// }

	// ft::RedBlackTree<ft::pair<int, int> >	rbt;

	// rbt.insert(ft::make_pair(42, 1));
	// rbt.insert(ft::make_pair(42, 2));
	// rbt.insert(ft::make_pair(42, 3));

	// // rbt.print_tree(rbt.get_root());

	// for (ft::RedBlackTree<ft::pair<int, int> >::iterator it = rbt.begin(); it != rbt.end(); it++) {
	// 	std::cout << it->second << std::endl;
	// }
}
